- MCP Client-Server model consists of three core architectural components. ​First is the MCP Host, which is the AI application responsible for coordinating and managing ​one or more MCP clients. ​The host component might take the form of a chatbot, an IDE-based code assistant, or ​a desktop application. 

- ​Next is the MCP Client, which maintains a connection to an MCP server and retrieves ​context for use by the MCP host. 

-​And lastly, there is the MCP Server, which serves as the location of the tools providing ​connectivity to databases, APIs, and local files and carrying out the tasks the agent ​must perform. ​It supplies contextual information to MCP clients. 

- ​Your MCP Host application is the component where your AI operates and interacts with ​people. ​This could take the form of a standalone desktop application, such as Claw Desktop or Visual ​Studio Code, or a more specialized AI-powered Integrated Development Environment IDE tool ​such as Cursor or Windsurf. ​Regardless of its form, the host application acts as the bridge between the user and the ​underlying AI model. ​It is responsible for capturing user input, whether that input is text, commands, or files, ​sending it to the AI agent for processing, and then presenting the AI's response in a clear, ​usable way. ​In addition, the host often handles important supporting functions, such as managing conversations, ​maintaining context, applying interface controls, and integrating with other tools or systems. ​In essence, although the AI model provides the intelligence, the host application provides ​the structure, interface, and user experience that make that intelligence accessible and ​practical to use.

- ​In the MCP ecosystem, all communication between the host and the server is mediated by a client. ​This MCP client runs within the host application and is responsible for translating user requests ​into a structured, standardized format that the open protocol can understand and process. ​The MCP client can be viewed as a unified gateway for communication. ​Instead of the host making separate, direct API calls to each external service, it sends ​structured MCP requests to the client using the JSON RPC protocol, allowing all interactions ​to be routed through a single, consistent interface. ​In addition, clients interpret server responses, perform error handling, and ensure that returned ​information is contextually relevant, accurate, and appropriate for the user's current interaction. ​Beyond simply relaying requests and responses, MCP clients also play a critical role in managing ​the session lifecycle. ​They handle interruptions, timeouts, reconnections, and clean session termination. 
​Though a single MCP host can support multiple clients, each individual client maintains ​a one-to-one relationship with a specific MCP server. ​Well-known examples of MCP clients include IBM's BAI, Microsoft's Copilot Studio, ​Claw Desktop, Claw.ai, Cursor.ai, Windsurf Editor, Klein, and LibreChat. ​These are just a few of the many available on the market. 

{Suppose, I designed an agent then that agent is what we call a mcp host, because this is what gets the user requests first and then tries to comprehend with the help of an llm, and then decides what tools to call and sends the request forward to the mcp client that might be built in within the agent and the client passes it to the server and server is where tools live. }

- ​The MCP server is an external service that supplies context to the LLM by translating ​user requests into concrete server-side actions. ​Examples of MCP integrations include Slack, GitHub, Git, Docker, or Websearch. ​These servers are typically GitHub repositories available in various programming languages, ​including C Sharp, Java, TypeScript, Python, and others. ​And they provide access to MCP tools. 


- ​MCP servers can also act as a bridge between LLM interface, provided by AI platforms such ​as IBM and OpenAI, and the MCP SDK. ​By doing this, they enable the creation of reusable MCP services that clients can access ​as standardized chat tools. ​

- MCP servers are highly flexible, as they can integrate with both internal systems and external ​resources or tools. ​MCP primitives are the core concept of MCP, defining what servers and clients can provide ​for one another. ​They determine the kinds of contextual information that can be shared with AI applications and ​the actions that can be carried out. 

- ​In MCP, servers can expose three types of core primitives as follows. ​
1. First, there are tools which offer functions that AI apps can invoke to perform activities ​such as calculations, sending messages, file operations, database calls, or data retrieval ​via API calls. 
2. ​Next are resources, which provide AI apps with access to contextual information from ​internal or external data sources. ​Resources return data only, such as file contents, database records, or API responses, and do ​not carry out actions or perform calculations. 
3. ​And thirdly are prompts, which are reusable templates and workflows that structure and ​streamline communication and interactions between the LLM and the server. 

- ​MCP is structured into two layers. 
1. ​The inner layer is the data layer, and it defines a JSON-RPC-based client-server protocol, ​encompassing lifecycle management and core primitives. 
2. ​Surrounding this is the transport layer between clients and servers, which is responsible ​for two-way communication channels and authentication between clients and servers. ​It handles connection establishment, message framing, and secure communication between ​MCP nodes. 

- ​In the client-to-server stream, MCP protocol messages are converted into JSON-RPC format, ​allowing for the transport of different data structures and their processing rules. 
- ​In the reverse, server-to-client stream, the returned responses in JSON-RPC format are ​converted back into MCP protocol messages. ​The three JSON-RPC message types include requests, responses, and notifications. ​Requests require a response from the server, whereas notifications do not. 

- ​Surrounding clients and servers, the MCP transport layer defines two primary transport mechanisms, ​both transporting messages in JSON-RPC 2.0 format. 
a. ​The first is Standard Input-Output, or STDIO. ​This mechanism is particularly effective for integrating local resources, as it provides ​simple, lightweight, synchronous message exchange with systems, such as local file systems, ​databases, and local APIs. 
b. ​The second is Streamable HTTP. ​This mechanism facilitates client-to-server communication over HTTP post, with optional ​support for server-sent events to enable streaming. ​It is designed for remote server interactions and accommodates standard HTTP authentication ​methods such as bearer tokens, API keys, and custom headers. ​The MCP standard advises using OAuth authorization framework to obtain authentication tokens. 

- SUMMARY: 

- ​The MCP client-server model consists of three core architectural components – the host, ​the client, and the server. ​The host application acts as the bridge between the user and the underlying AI model. 
- ​The MCP client runs within the host application and is responsible for translating user requests ​into a structured, standardized format. ​The - MCP server is an external service that supplies context to the LLM by translating ​user requests into server-side actions. ​MCP primitives define what clients and servers can provide for one another and consist of ​tools, resources, and prompts. ​MCP is structured into two layers – the data layer and the transport layer. ​The data layer defines a JSON-RPC-based client-server protocol which encompasses lifecycle management ​and fundamental primitives. ​The transport layer is responsible for two-way communication channels and authentication ​between clients and servers. ​The MCP transport layer defines two primary transport mechanisms, both transmitting messages ​in JSON-RPC 2.0 format. 
​The STDIO transport mechanism is particularly effective for integrating local resources. ​And the streamable HTTP transport mechanism facilitates client-to-server communication ​over HTTP post and is designed for remote server interactions. 
