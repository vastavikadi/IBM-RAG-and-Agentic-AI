- ​Fast MCP servers behave like remote function libraries. Instead of importing locally, you ​connect to services anywhere. Here we have a simple MCP server to perform mathematical ​calculations. The client is your code that connects to an MCP server and is represented ​by a Python object. Each server has tools, like add and multiply. 

- ​The server can run anywhere. Your code talks to it over the network. We call tools using ​the tool name and parameters. The tool name is add, and we pass parameters a and b with ​values 1 and 2. The parameters are set, the add tool is selected, and the result 3 is ​transmitted back via the transport.

- ​The first transport is STDIO, which stands for Standard Input Output. This is used to ​communicate with a server running on your own system.
​The client uses STDIN, which stands for Standard Input, which sends messages to the MCP server. ​And the server uses STDOUT or Standard Output to communicate back to the client. ​STDIO is ideal for testing in applications that don't need network connectivity. 

- ​The second transport is HTTP, which stands for Hypertext Transfer Protocol. This is used ​to communicate with a server over a network connection, rather than just your own system. ​The client sends HTTP requests to the MCP server, and the server responds with HTTP responses. ​The HTTP transport is suited for remote setups, where the client and server may run on different ​machines or across the internet. It enables MCP servers to integrate with web tools and ​cloud services. 

- STDIO and HTTP. ​Later, we'll also introduce several other transport options. 

- ​Context 7 provides up-to-date code documentation and API references formatted specifically ​for LLMs. You input library names, search queries, or requests, ​and the output it returns can be up-to-date code snippets and documentation formatted ​in Markdown for LLMs, library trust scores and compatibility details, or API references ​and function descriptions. 
a. When we access the MCP server, we input the ​tool we want, in this case, resolve library ID and parameters with the library name. ​The tool then selects the appropriate documentation, in this case, a list of library names with ​a lot of other info, in this case, scikit-learn. 
b. The second tool is getLibraryDocs. The parameter ​is the output from before. The MCP server will select the tool and send ​the appropriate docs.

- ​We need to import some libraries. First, we import client from fastmcp and stdio-transport ​from the fastmcp.client.transports module. The client library handles our connection ​to the MCP server, and the stdio-transport library enables communication with servers ​running locally on your system using standard input-output. ​We need to create an stdio-transport object to establish the connection with the client. ​It connects your Python code to the MCP server via the stdin and stdout transports. ​The arguments include the npx command that downloads and runs the context 7 MCP server ​– think about it like a PIP install for JavaScript. It also includes the args command ​with the dash y flag to auto-confirm installation prompts.
​At upstash or context-7 mcp is the name of the server package we discussed earlier. ​We pass the stdio-transport object into the client constructor to create the stdio-client ​object. We use the stdio-client object to communicate ​with the MCP server. As shown in the diagram, the stdio-client ​encapsulates the connection with the box around the client and transport. ​Since communication between stdio-client and the MCP server can take some time, Python ​uses async methods to prevent freezing, which allows other tasks to run concurrently. ​The async with stdio-client as client command creates a context manager that automatically ​handles connection setup and cleanup after the code in the indent has run. ​The client.list-tools command requests available tools from the server. 
​And the await keyword pauses this task until the server responds, while allowing other ​tasks to continue. The tools variable stores the list of available ​functions we discovered that we can call. The MCP server will send a list of tool objects ​as shown in the image. We show it as a list, but what is actually ​transmitted depends on the protocol used. Let's examine the tools list that contains ​the two tool objects returned by the MCP server. The number of tools corresponds to the list ​length. To access info for the first tool, we use ​the first element of the list and examine the tool attribute .name. 
​We get resolve-library-id, which searches for library identifiers. ​Similarly, for the second element, we use the name attribute to get get-library-docs, ​which retrieves actual documentation. We can access detailed tool information by ​examining the first element of the tools list using the description attribute. ​This tool resolves package names to context 7 compatible library IDs, analyzes queries ​for relevance, and returns the most suitable library match based on name similarity, documentation ​coverage, and trust scores. The input schema is the second attribute ​and defines the required parameters for this tool. ​For example, this tool requires a library name parameter of the string type and a description ​explaining what the tool does. In this case, the description is, library ​name to search for and retrieve a context 7 compatible library ID. 


- ​You can also see that library name is listed as a required parameter. ​Let's call the resolve-library-id tool on the FastMCP library. ​We'll use the client.call-tool method to actually access the MCP server. ​When calling the call-tool method with an async context manager, we call the method ​with three arguments, the tool name, resolve-library-id, the library name parameter, and the library ​we want, which is FastMCP. Printing the output shows tool documentation ​for resolve-library-id. It converts library names to context 7 IDs and returns matching ​options with trust scores. In this example, we searched for FastMCP ​and got multiple FastMCP library results to choose from. 
​Using the context 7 compatible library ID from the previous tool, we can obtain documentation. ​Using the call-tool method with async, we use the get-library-docs tool. ​The tool takes input parameters, the library ID, slash punkpi slash fastmcp, and a token ​limit of 5,000 to control output length. We can print out the response, which displays ​code snippets with titles, descriptions, and source links for working with FastMCP. ​So far, we've seen how to use the stdio transport. 

- Now, let's see how to use the ​HTTP transport instead. The code for using HTTP transport is almost ​identical to the one we saw previously for stdio. So again, we start by importing the ​streamable HTTP transport library from the fastmcp.client.transports module. 
​We create a transport layer using the streamable HTTP transport object, HTTP underscore transport. ​The parameter URL connects to the MCP server's HTTP endpoint at the specified URL. This ​enables bidirectional communication over standard web protocols anywhere. ​The HTTP transport object is used as input for the client constructor, creating an HTTP ​underscore client. Once the HTTP underscore client is set up, ​it's just like the stdio client. We use async with HTTP underscore client as a client pattern. ​The client.list underscore tools method returns identical tool objects. 
​We use client.call underscore tool, resolve library ID, to find library IDs and search ​for FastMCP options. And we use client.call underscore tool, get ​library docs, to retrieve code snippets using the library ID. ​Only the transport differs, the rest is identical. 

- Here's a concise summary of the other MCP ​transports that can be used. 
1. Server Sent Events, or SSE, is a legacy transport ​for HTTP communication and is now replaced by the streamable HTTP transport. ​It uses asymmetrical channel communication. 
b. In-memory transport establishes a direct connection ​within the same Python process, eliminating network overhead. 
​It employs direct function calls between the different components and is primarily used ​for development and testing purposes. 

- But the bottom line is, you shouldn't need ​to use these as stdio and HTTP cover most real-world MCP use cases. ​When clients receive tools, they are structured by MCP. ​Essentially, this means that clients require some sort of adaptation to MCP to understand ​and use the tools. For example, LangChain has an MCP adapters ​library to convert MCP tools into LangChain-structured tools compatible with LangChain and LangGraph. ​MCP is a standard that requires clients to conform to its specifications to both access ​and invoke its capabilities. Remember that the term tool is used across ​different agentic frameworks and protocols, it doesn't fit to a singular ecosystem. 
​There are different clients that adapt tools to different scenarios. ​So you'll see the other ways of instantiating clients as you progress in the course. 


- SUMMARY:
- ​In this video, you learned that MCP servers act as a bridge between LLMs and external ​tools or services. They support two main transports, stdio, ​used to connect with local servers via subprocesses, and HTTP, used to reach remote servers over ​the internet. The same client API works across both transports, ​making integration consistent and portable. Context 7 provides LLM-ready documentation ​that models can consume directly. Client connections use the async-await pattern, ​enabling non-blocking communication with servers. 
​And MCP servers are the foundation for the next generation of LLM capabilities. 