- LlamaIndex used for document ingestion and chunking. ​LlamaIndex is a framework for building LLM-powered context augmentation. ​Context augmentation is the process of making your data available to an LLM, allowing the ​LLM to perform a particular task while grounding its responses in the provided context. 

- ​Typical use cases include question answering with Retrieval-Augmented Generation or RAG. Chatbots which extend the basic RAG pipeline ​by allowing for multiple back-and-forth queries and answers, allowing the LLM to ask for clarification ​or answer follow-up questions, and document understanding and data extraction, which allow ​an LLM to read natural language and semantically identify important details such as names, dates, ​addresses, and figures from large amounts of structured or unstructured data. 

- ​The RAG process begins by loading and chunking the source documents. ​These document chunks are then embedded using the embedding model, which embeds text as ​equal-sized vectors, one vector per text chunk. ​These vectors then go into a vector store which is a database that is well-suited to ​storing and manipulating vectors. ​When a prompt is provided by the user, that prompt is then embedded using the same model ​as the source documents and a retriever is used to retrieve the relevant text from the ​vector store. 
​The retrieved text is then used to augment the prompt, and this augmented prompt is then ​fed into an LLM to provide a context-aware response that considers the most relevant ​data from the source documents.

- ​Let's begin with the first RAG step, which involves loading textual data file objects ​into a LlamaIndex document, which is LlamaIndex's generic container for document sources. ​You can load textual data from a variety of formats including text files, PDF files, markdown ​files, comma-separated or CSV files, JSON files, and HTML files. ​In addition to supporting the loading of local documents, LlamaIndex also offers numerous ​connectors for seamless integration with various databases and cloud storage services.

- ​First, you create an object of the document class by passing the text Hello LlamaIndex ​into the document class's text parameter. ​Then, by calling the resulting document object's dict method, you can inspect the items stored ​in the document. 
​Note that a LlamaIndex document has several key components. ​An ID that uniquely identifies the document, a placeholder for embedding if you decide ​to embed the whole document, a metadata dict where you can store metadata associated with ​the document, such as its origin or date of creation, a relationships dict that links ​the document to other related items such as other documents, and the text that is present ​in the document. ​Thus, the document class serves as a container that stores a document's text in addition ​to providing each document with a unique ID and stores the document's metadata and relationships ​in dictionaries. 

- ​Now let's look at how to load documents from local storage. ​LlamaIndex provides a powerful document loader called SimpleDirectoryReader that can load ​many file types including plain text, markdown, CSV, and PDF files. ​To use the SimpleDirectoryReader file loader, import it from Llama_index.core ​Then, you can load all files in the directory by passing that directory's path as the ​first argument to SimpleDirectoryReader and calling the load_data method. ​If on the other hand, you would like to load all files from a path with all of its subdirectories, ​you can pass recursive = True as a parameter to SimpleDirectoryReader. 
​Alternatively, if you would like to load specific files, you can pass the file's path as a ​list to the input_files parameter. ​Finally, to load specific file types only, pass a list of requested file type extensions ​to the required _ext parameter. ​Note that SimpleDirectoryReader outputs a list of LlamaIndex document class objects ​of the type you just saw. 

- ​A LlamaIndex node is simply a text chunk. ​Chunking long text helps retain the specific context of each segment, leading to more precise ​and relevant embeddings. ​LlamaIndex provides an easy-to-use but surprisingly effective text chunker called SentenceSplitter. ​SentenceSplitter uses a recursive method, splitting long text excessively based on specific ​characters such as newline characters and periods. 
​To use SentenceSplitter, first import it from Llama_index.core.node_parser. ​Then, define the node parser by passing the chunk_size and chunk_overlap parameters. ​The chunk_size parameter controls the maximum size of each chunk in tokens and ​the chunk_overlap controls the maximum number of tokens that can overlap ​from one chunk to the next. ​Then use the get_nodes_from_documents method to split ​documents into individual nodes. ​Note that the node parser returns a list of LlamaIndex text node instances, which are ​similar in structure to the structure of LlamaIndex document instances that you saw previously.

- ​In addition to SentenceSplitter, LlamaIndex provides many other splitters, such as Semantic ​Splitter that splits wherever sentence similarity falls below a certain threshold, or a wrapper ​for Langchain Splitters that allows you to use any Langchain Splitters in LlamaIndex.